'use strict';

// Persona functionality is disabled; Tavus integration now relies on replica_id only.
async function ensurePersonaConfigured() {
  return null;
}

function getPersonaId() {
  return null;
}

module.exports = {
  ensurePersonaConfigured,
  getPersonaId
};
